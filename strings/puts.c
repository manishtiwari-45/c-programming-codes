#include<stdio.h>
#include<string.h>

int main()
{
    char str[] = "Manish is the best";
    puts(str); // print "string" to screen
    printf("%s\n", str);

    // gives automatically a "\n"

    puts("Manish");
}